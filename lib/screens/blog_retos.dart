import 'package:flutter/material.dart';

class BlogRetosScreen extends StatelessWidget {
  const BlogRetosScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('BlogRetosScreen'),
      ),
    );
  }
}
