import 'package:flutter/material.dart';

class HablaConEmiScreen extends StatelessWidget {
  const HablaConEmiScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('HablaConEmiScreen'),
      ),
    );
  }
}
