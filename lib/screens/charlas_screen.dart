import 'package:flutter/material.dart';

class CharlasScreen extends StatelessWidget {
  const CharlasScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('CharlasScreen'),
      ),
    );
  }
}
