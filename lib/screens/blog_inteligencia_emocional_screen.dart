import 'package:flutter/material.dart';

class BlogInteligenciaEmocionalScreen extends StatelessWidget {
  const BlogInteligenciaEmocionalScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('BlogInteligenciaEmocionalScreen'),
      ),
    );
  }
}
