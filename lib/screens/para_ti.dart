import 'package:flutter/material.dart';

class ParaTiScreen extends StatelessWidget {
  const ParaTiScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ParaTiScreen'),
      ),
    );
  }
}
