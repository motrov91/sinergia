import 'package:flutter/material.dart';

class UserConfig extends StatelessWidget {
  const UserConfig({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('UserConfig'),
      ),
    );
  }
}
